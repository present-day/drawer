/* Default handle bar: extension .pcss avoids tsup's PostCSS onLoad (/.css$/) which breaks CSS module exports. */
.indicator {
  display: block;
  height: 0.25rem;
  width: 100px;
  border-radius: 9999px;
  background-color: rgb(156 163 175 / 0.25);
}
